# ──────────────────────────────────────────────────────────────
# Sahayak API — Python dependencies
# ──────────────────────────────────────────────────────────────
# Pinned versions keep our builds reproducible (a recruiter or future-you
# gets the exact same behaviour). We'll add more (langgraph, LLM SDKs,
# pdfplumber, torch) as we build later sprints.

# ── Web framework ──
fastapi==0.115.6
# ASGI server that actually runs FastAPI (async, production-grade).
uvicorn[standard]==0.34.0

# ── Settings & validation ──
# Loads our .env file into typed settings so we don't hardcode secrets.
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# ── Database ──
# Modern Postgres driver (psycopg3, not the old psycopg2).
psycopg[binary]==3.2.13
# SQLAlchemy = the standard Python ORM (we'll use it in Sprint 4).
SQLAlchemy==2.0.36

# ── HTTP / networking helpers ──
httpx==0.28.1

# ── Agent orchestration ──
# LangGraph wires our 5 agents into a controllable, observable pipeline
# (shared State, nodes, edges). The heart of the "agentic AI" layer.
langgraph==1.2.10

# ── File upload & PDF ingestion (Day 1.2) ──
# Parses multipart/form-data — FastAPI REQUIRES it for UploadFile to work
# (classic gotcha: without it, uploads crash at runtime, not at import).
python-multipart==0.0.20
# Extracts the text layer from born-digital PDFs, page by page.
pdfplumber==0.11.4

# ── OCR fallback for scanned PDFs (Day 1.3) ──
# Thin wrapper that shells out to the Tesseract binary — a SYSTEM dep,
# installed via apt in the Dockerfile (not here; pip can't provide it).
pytesseract==0.3.13
# Renders PDF pages into bitmap images — OCR reads pictures, not text layers.
pypdfium2==4.30.0
